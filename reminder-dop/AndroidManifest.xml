<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unb.cic.reminders2"
    android:versionCode="1"
    android:versionName="0.0.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.Reminder" >
        <activity
            //#if (base && fixedDate) && !manageCategory && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddate.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && staticCategory) && !manageCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatestaticcategory.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && manageCategory) && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategory.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && manageCategory && priority) && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatepriority.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && priority) && !manageCategory && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategorypriority.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && googleCalendar) && !manageCategory && !staticCategory && !dateRange && !priority && !search
            android:name="reminder.basefixeddategooglecalendar.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && fixedDate && manageCategory && priority && googleCalendar) && !staticCategory && !dateRange && !search
            android:name="reminder.basefixeddatemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.ReminderMainActivity"

            //#elif (base && dateRange) && !manageCategory && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterange.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && staticCategory) && !manageCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangestaticcategory.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && manageCategory) && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategory.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && manageCategory && priority) && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangepriority.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && priority) && !manageCategory && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategorypriority.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && googleCalendar) && !manageCategory && !staticCategory && !fixedDate && !priority && !search
            android:name="reminder.basedaterangegooglecalendar.br.unb.cic.reminders.ReminderMainActivity"
            //#elif (base && dateRange && manageCategory && priority && googleCalendar) && !staticCategory && !fixedDate && !search
            android:name="reminder.basedaterangemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.ReminderMainActivity"
            //#endif
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            //#if (base && fixedDate) && !manageCategory && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddate.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && staticCategory) && !manageCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatestaticcategory.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && manageCategory) && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategory.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority) && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatepriority.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && priority) && !manageCategory && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategorypriority.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && googleCalendar) && !manageCategory && !staticCategory && !dateRange && !priority && !search
            android:name="reminder.basefixeddategooglecalendar.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority && googleCalendar) && !staticCategory && !dateRange && !search
            android:name="reminder.basefixeddatemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.AddReminderActivity"

            //#elif (base && dateRange) && !manageCategory && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterange.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && staticCategory) && !manageCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangestaticcategory.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && manageCategory) && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategory.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && manageCategory && priority) && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangepriority.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && priority) && !manageCategory && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategorypriority.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && googleCalendar) && !manageCategory && !staticCategory && !fixedDate && !priority && !search
            android:name="reminder.basedaterangegooglecalendar.br.unb.cic.reminders.view.AddReminderActivity"
            //#elif (base && dateRange && manageCategory && priority && googleCalendar) && !staticCategory && !fixedDate && !search
            android:name="reminder.basedaterangemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.AddReminderActivity"
            //#endif
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/title_activity_reminder_add"
            android:screenOrientation="landscape" >
        </activity>
        <activity
            //#if (base && fixedDate) && !manageCategory && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddate.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && staticCategory) && !manageCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatestaticcategory.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && manageCategory) && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategory.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority) && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatepriority.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && priority) && !manageCategory && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategorypriority.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && googleCalendar) && !manageCategory && !staticCategory && !dateRange && !priority && !search
            android:name="reminder.basefixeddategooglecalendar.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority && googleCalendar) && !staticCategory && !dateRange && !search
            android:name="reminder.basefixeddatemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.EditReminderActivity"

            //#elif (base && dateRange) && !manageCategory && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterange.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && staticCategory) && !manageCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangestaticcategory.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && manageCategory) && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategory.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && manageCategory && priority) && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangepriority.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && priority) && !manageCategory && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategorypriority.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && googleCalendar) && !manageCategory && !staticCategory && !fixedDate && !priority && !search
            android:name="reminder.basedaterangegooglecalendar.br.unb.cic.reminders.view.EditReminderActivity"
            //#elif (base && dateRange && manageCategory && priority && googleCalendar) && !staticCategory && !fixedDate && !search
            android:name="reminder.basedaterangemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.EditReminderActivity"
            //#endif
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/title_activity_reminder_edit"
            android:screenOrientation="landscape" >
        </activity>
        <activity
            //#if (base && fixedDate) && !manageCategory && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddate.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && staticCategory) && !manageCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatestaticcategory.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && manageCategory) && !staticCategory && !priority && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategory.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority) && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatepriority.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && priority) && !manageCategory && !staticCategory && !dateRange && !googleCalendar && !search
            android:name="reminder.basefixeddatemanagecategorypriority.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && googleCalendar) && !manageCategory && !staticCategory && !dateRange && !priority && !search
            android:name="reminder.basefixeddategooglecalendar.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && fixedDate && manageCategory && priority && googleCalendar) && !staticCategory && !dateRange && !search
            android:name="reminder.basefixeddatemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.ExternalAddReminderActivity"

            //#elif (base && dateRange) && !manageCategory && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterange.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && staticCategory) && !manageCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangestaticcategory.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && manageCategory) && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategory.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && manageCategory && priority) && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangepriority.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && priority) && !manageCategory && !staticCategory && !fixedDate && !googleCalendar && !search
            android:name="reminder.basedaterangemanagecategorypriority.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && googleCalendar) && !manageCategory && !staticCategory && !fixedDate && !priority && !search
            android:name="reminder.basedaterangegooglecalendar.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#elif (base && dateRange && manageCategory && priority && googleCalendar) && !staticCategory && !fixedDate && !search
            android:name="reminder.basedaterangemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.view.ExternalAddReminderActivity"
            //#endif
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/title_activity_reminder_edit"
            android:screenOrientation="landscape" >
            <intent-filter>

                //#if (base && fixedDate) && !manageCategory && !staticCategory && !priority && !dateRange && !googleCalendar && !search
                <action android:name="reminder.basefixeddate.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && staticCategory) && !manageCategory && !priority && !dateRange && !googleCalendar && !search
                <action android:name="reminder.basefixeddatestaticcategory.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && manageCategory) && !staticCategory && !priority && !dateRange && !googleCalendar && !search
                <action android:name="reminder.basefixeddatemanagecategory.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && manageCategory && priority) && !staticCategory && !dateRange && !googleCalendar && !search
                <action android:name="reminder.basefixeddatepriority.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && priority) && !manageCategory && !staticCategory && !dateRange && !googleCalendar && !search
                <action android:name="reminder.basefixeddatemanagecategorypriority.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && googleCalendar) && !manageCategory && !staticCategory && !dateRange && !priority && !search
                <action android:name="reminder.basefixeddategooglecalendar.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && fixedDate && manageCategory && priority && googleCalendar) && !staticCategory && !dateRange && !search
                <action android:name="reminder.basefixeddatemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.ADD_REMINDER" />

                //#elif (base && dateRange) && !manageCategory && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
                <action android:name="reminder.basedaterange.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && staticCategory) && !manageCategory && !priority && !fixedDate && !googleCalendar && !search
                <action android:name="reminder.basedaterangestaticcategory.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && manageCategory) && !staticCategory && !priority && !fixedDate && !googleCalendar && !search
                <action android:name="reminder.basedaterangemanagecategory.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && manageCategory && priority) && !staticCategory && !fixedDate && !googleCalendar && !search
                <action android:name="reminder.basedaterangepriority.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && priority) && !manageCategory && !staticCategory && !fixedDate && !googleCalendar && !search
                <action android:name="reminder.basedaterangemanagecategorypriority.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && googleCalendar) && !manageCategory && !staticCategory && !fixedDate && !priority && !search
                <action android:name="reminder.basedaterangegooglecalendar.br.unb.cic.reminders.ADD_REMINDER" />
                //#elif (base && dateRange && manageCategory && priority && googleCalendar) && !staticCategory && !fixedDate && !search
                <action android:name="reminder.basedaterangemanagecategoryprioritygooglecalendar.br.unb.cic.reminders.ADD_REMINDER" />
                //#endif

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
