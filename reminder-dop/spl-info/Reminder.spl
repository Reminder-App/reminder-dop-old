SPL Reminder {

	Features = {Service, StaticCategory}

	Deltas = {dService, dStaticCategory}

	Constraints {
		
	}

	Partitions {
		{dService} when (Service);
		{dStaticCategory} when (StaticCategory);
	}

	Products {
		Base = {Service};
		StaticCategory = {Service, StaticCategory};
	}
}
