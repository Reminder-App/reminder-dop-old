SPL Reminder {

	Features = {Services, Category, GoogleCalendar}

	Deltas = {dService, dGoogleCalendar, dCategory}

	Constraints {
		
	}

	Partitions {
		{dService} when (Services);
		{dService, dCategory} when (Category);
		{dService, dGoogleCalendar} when (GoogleCalendar);
		
	}

	Products {
		Base = {Services};
		AddCategory = {Category};
		GoogleCalendar = {GoogleCalendar};
		Full = {Services, Category, GoogleCalendar};
	}
}
